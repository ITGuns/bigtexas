# Paths are anchored with a leading slash on purpose: a bare "data" would also
# match src/data, which silently removed the site's content from the build.
/.env
/.env.*
!/.env.example
/node_modules
/dist
/.astro
/.vercel
/data
/shots
/tests
